<h2>Articles</h2>
<% @articles.each do |article| %>
  <p class="section-title"><%= article.title %></p>
    <p><%= article.text %></p>
    <p>Author:<i><%= article.author %></i></p>
    <div>
      <%= link_to 'Show', article_path(article) %>
    </div>
    <hr />
<% end %>